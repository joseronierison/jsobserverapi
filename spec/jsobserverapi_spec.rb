require 'spec_helper'

describe Jsobserverapi do
  it 'has a version number' do
    expect(Jsobserverapi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
